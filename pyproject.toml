[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "oceanum"
description = "Library for oceanum.io services"
readme = "README.rst"
license = { file = "LICENSE" }
keywords = ["oceanum", "datamesh", "oceanum.io"]
authors = [
  { name = "Oceanum Developers", email="developers@oceanum.science" },
]
maintainers = [
  {name = "Oceanum Developers", email = "developers@oceanum.science"},
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Natural Language :: English",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Physics",
    "Topic :: Scientific/Engineering :: Visualization",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
]
requires-python = ">=3.8"
dependencies = [
  "aiohttp",
  "click",
  "cftime",
  "dask",
  "decorator",
  "fsspec",
  "geojson-pydantic",
  "geopandas",
  "h5netcdf",
  "numpy",
  "orjson",
  "pandas",
  "platformdirs",
  "pyarrow",
  "pydantic[email]",
  "python-jsonpath",
  "requests",
  "shapely",
  "tabulate",
  "xarray",
  "zarr",
  "humanize"
]
dynamic = ["version"]

[project.optional-dependencies]
test = [
  "pytest",
  "pytest-env",
]
extra = [
    "xarray_video",
]
docs = [
  "autodoc-pydantic",
  "pydata-sphinx-theme",
  "sphinx",
  "sphinxcontrib-programoutput",
]

[project.urls]
"Homepage" = "https://oceanum.io/"
"Source" = "https://github.com/oceanum-io/oceanum-python"
"Documentation" = "https://oceanum-python.readthedocs.io/en/latest/"
"Tracker" = "https://github.com/oceanum-io/oceanum-python/issues"

[project.scripts]
oceanum = "oceanum.__main__:main"

[project.entry-points."fsspec.specs"]
"oceanum" = "oceanum.storage:FileSystem"

[project.entry-points."oceanum.cli.main"]
"main" = "oceanum.cli.main"

[project.entry-points."oceanum.cli.auth"]
"auth" = "oceanum.cli.auth"

[project.entry-points."oceanum.cli.run.main"]
"run" = "oceanum.cli.run.main"
"project" = "oceanum.cli.run.project"
"route" = "oceanum.cli.run.route"
"user" = "oceanum.cli.run.user"

[project.entry-points."oceanum.cli.datamesh.main"]
"datamesh" = "oceanum.cli.datamesh.main"

[project.entry-points."oceanum.cli.storage.main"]
"storage" = "oceanum.cli.storage.main"

[tool.setuptools.dynamic]
version = {attr = "oceanum.__version__"}

[tool.setuptools.packages.find]
where = ["src/"]
include = [
  "oceanum",
  "oceanum.datamesh",
  "oceanum.storage",
  "oceanum.cli.main",
  "oceanum.cli.common",
  "oceanum.cli.auth",
  "oceanum.cli.datamesh",
  "oceanum.cli.storage",
]
exclude = ["tests", "docs"]

[tool.distutils.bdist_wheel]
universal = true

[tool.pytest.ini_options]
required_plugins = "pytest-env"
env = [
  "DATAMESH_TOKEN = aa54ff999abf474c0c1584fb6e97134249c3e437",
  "STORAGE_SERVICE = https://storage.oceanum.io",
]